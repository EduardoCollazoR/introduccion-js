//objeto math
let resultado;

resultado = Math.PI;
resultado = Math.round(2.7);
resultado = Math.ceil(2.2); //Ceil siempre redondea hacia arriba
resultado = Math.floor(2.5); //Floor siempre redondea hacia abajo

resultado = Math.sqrt(144); //raiz cuadrada
resultado - Math.abs(-3); //negativo a positivo
resultado = Math.min(2, 4, 5, 32, 6, 5); // retorna el menor
resultado = Math.max(2, 4, 5, 32, 6, 5); //retorna el mayor
resultado = Math.random(); // numero random

resultado = Math.floor(Math.random() * 30); // numero random

console.log(resultado);
